<div class="posts-wrapper">

	<% @posts.each_with_index do |post, index| %>
		<% if index == 0 %>
			<div class="big-post post-box">
				<h2><%= post.title %></h2>
				<p><%= post.description %></p>
				<%= @markdown.render(post.content).html_safe %>
			</div>
		<% elsif index > 0 || index < 3%>
			<div class="mid-post post-box">
				<h2><%= post.title %></h2>
				<p><%= post.description %></p>
				<%= @markdown.render(post.content) %>
			</div>
		<% else %>
			<div class="small-post post-box">
				<h2><%= post.title %></h2>
				<p><%= post.description %></p>
				<p><%= post.content %></p>
			</div>
		<% end %>
	<% end %>
</div>